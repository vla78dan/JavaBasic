package com.hyperskill.ex.fourty.whileloop;

import java.util.Scanner;

public class MaxTemp {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int maxTemp = 0;

        if(maxTemp >= 100){
            System.out.println("0");
        }else{

        }
    }
}
/*
 Scanner scanner = new Scanner(System.in);
        int maxTemperature = -1; // Инициализируем максимальную температуру значением ниже 0
        int temperature;

        System.out.println("Введите последовательность показаний температуры (в градусах Цельсия). Введите 100 или более для завершения:");

        while (true) {
            temperature = scanner.nextInt(); // Считываем температуру

            if (temperature >= 100) { // Проверяем условие остановки
                break; // Выходим из цикла, если температура 100 или более
            }

            if (temperature > maxTemperature) { // Проверяем, является ли текущая температура максимальной
                maxTemperature = temperature; // Обновляем максимальную температуру
            }
        }

        if (maxTemperature != -1) { // Проверяем, была ли введена хотя бы одна температура
            System.out.println("Наивысшее показание температуры: " + maxTemperature + "°C");
        } else {
            System.out.println("Температура не была введена.");
        }

        scanner.close(); // Закрываем сканер
    }
}
Объяснение кода:
Импортируем класс Scanner: Он используется для считывания ввода от пользователя.
Создаем основной класс TemperatureTracker: В этом классе будет находиться основной метод.
Основной метод main:
Создаем объект Scanner для считывания ввода.
Инициализируем переменную maxTemperature значением -1, чтобы отслеживать максимальную температуру.
Запускаем бесконечный цикл while, который будет продолжаться до тех пор, пока не будет введено значение 100 или более.
Считываем температуру и проверяем, превышает ли она 100. Если да, выходим из цикла.
Если температура меньше 100 и больше текущего максимума, обновляем значение maxTemperature.
Вывод результата:
После выхода из цикла проверяем, была ли введена хотя бы одна температура. Если да, выводим наивысшее значение; если нет — выводим сообщение о том, что температура не была введена.
Напишите программу Java, которая считывает последовательность показаний температуры в градусах Цельсия до тех пор, пока не будет обнаружено показание 100 градусов или более. Ваша программа должна выводить наивысшее показание температуры, зафиксированное до этого момента. Вы должны принять входные данные в виде последовательности целочисленных значений температуры и вывести наивысшее значение перед условием остановки.

ПОДСКАЗКА : Предположим, что вы имеете дело только с положительными температурами
 */