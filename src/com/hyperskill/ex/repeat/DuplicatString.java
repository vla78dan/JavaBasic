package com.hyperskill.ex.repeat;

public class DuplicatString {
    public static void main(String[] args) {
        /**
         * import java.util.Scanner;
         *
         * public class DuplicateCharacters {
         *     public static void main(String[] args) {
         *         // Создаем объект Scanner для считывания ввода
         *         Scanner scanner = new Scanner(System.in);
         *
         *         // Считываем строку из стандартного ввода
         *         System.out.print("Введите строку: ");
         *         String input = scanner.nextLine();
         *
         *         // Создаем StringBuilder для формирования новой строки
         *         StringBuilder result = new StringBuilder();
         *
         *         // Проходим по каждому символу входной строки
         *         for (char c : input.toCharArray()) {
         *             // Дублируем символ и добавляем в результат
         *             result.append(c).append(c);
         *         }
         *
         *         // Выводим результат
         *         System.out.println("Результат: " + result.toString());
         *
         *         // Закрываем сканер
         *         scanner.close();
         *     }
         * }
         * Как работает программа:
         * Программа использует класс Scanner для считывания строки из стандартного ввода.
         * Создается объект StringBuilder, который будет использоваться для формирования новой строки.
         * Программа проходит по каждому символу входной строки с помощью цикла for-each, дублирует его и добавляет в StringBuilder.
         * В конце программа выводит полученную строку.
         * Запуск программы:
         * Скопируйте код в файл с именем DuplicateCharacters.java.
         * Скомпилируйте программу с помощью команды javac DuplicateCharacters.java.
         * Запустите программу с помощью команды java DuplicateCharacters.
         * Введите строку, и программа выведет результат.
         *
         * https://hyperskill.org/learn/step/45472#comment
         *
         *
         *
         */
    }
}
